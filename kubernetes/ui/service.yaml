apiVersion: v1
kind: Service
metadata:
  name: stylehub-ui-service
  namespace: stylehub
  labels:
    app: stylehub-ui
    component: frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: stylehub-ui
